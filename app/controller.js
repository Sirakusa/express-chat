const { RoomRepository } = require('./room.js');
const { MessageRepository } = require('./message.js');

const message = new MessageRepository();
const room = new MessageRepository();


function conversation(req,res){
    
}